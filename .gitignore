# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# secrets
.env
.env.*

# eval scratch: results/ IS committed (it is the deliverable); its scratch is not
results/**/raw_trajectories/
eval/.cache/
report.xml

# swebench harness scratch -- per-instance patch/eval/test logs, unbounded
# growth on every real run. results/ (committed) is the summarized verdict;
# this is the working directory behind it.
logs/
